# Cloud provider declared here

# Configurations that should apply to all infrastructure by the provider (AWS) such as the region they reside in declared here
provider "aws" { 
  region = "eu-west-1" 
}

